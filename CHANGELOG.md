# @younggichoi/kis-trader

## 0.5.3

### Patch Changes

- 731a52f: 같은 주문 거부 경고가 하루 종일 반복 발송되던 문제를 고친다.
  
  - **주문 거부 알림 차단기 (`src/notify/order_reject.py`)** — 2026-08-28 모의투자 계좌가 주문 권한을 잃자(`모의투자 주문이 불가한 계좌입니다`) monitor가 5분마다, cash-deploy가 30분마다 같은 주문을 재시도했고 거부 한 건마다 텔레그램 경고가 나갔다. 3일 만에 글자까지 똑같은 알림이 151건 쌓여 정작 봐야 할 알림을 덮었다. 억제 기준을 KIS 거부 메시지 화이트리스트가 아니라 **문구의 반복**으로 잡는다 — 어떤 사유가 재시도로 풀리는지 우리가 다 알 수 없지만, 하루 종일 그대로 반복되는 경고는 사유와 무관하게 첫 건만 보면 충분하다. 문구에 종목명이 들어 있어 종목별로는 각각 한 번씩 알림이 간다. 날짜가 바뀌면 다시 한 번 알린다 — 장애가 이어지고 있다는 사실은 매일 알아야 한다.
  - **주문 재시도 자체는 막지 않는다** — 장중에 계좌를 복구하면 다음 틱부터 바로 주문이 나가야 한다. 억제되는 것은 텔레그램 알림뿐이고, 거부는 매 건 `log.warning`으로 남는다.
  - 상태 파일(`data/logs/.order_reject_state.json`)은 monitor/orchestrator/cash-deploy/dip-buy가 서로 다른 프로세스에서 갱신하므로 `fcntl.flock`으로 직렬화한다. 파일을 읽거나 쓸 수 없으면 억제 없이 알린다 — 침묵보다 중복이 낫다.
- fbbc055: 텔레그램 알림 뮤트 스위치를 추가한다.
  
  - **뮤트 플래그 (`data/telegram_muted`)** — 파일이 존재하면 `src/notify/telegram.py`의 `send()`와 `src/signal/notify/telegram_bot.py`의 `send_message()`가 전송 없이 `[telegram muted]` 로그만 남긴다. 키 재발급 등으로 알림을 잠시 꺼야 할 때 파일 생성/삭제만으로 켜고 끈다 — 코드 변경도 프로세스 재시작도 필요 없다 (매 전송 시점에 파일 존재를 확인한다).
  - 시그널 이식 트리는 `src.notify`를 import할 수 없어서(test_vendored_port 계약) 플래그 경로를 인라인으로 둔다.
  - 텔레그램 에이전트(사용자 명령 응답)는 뮤트 대상이 아니다 — 꺼지는 것은 알림뿐이다.

## 0.5.2

### Patch Changes

- 2909a58: 릴리즈 태그가 원격에 올라가지 않던 문제와 KIS 호출 간격이 프로세스 간에 조율되지 않던 문제를 고친다.
  
  - **릴리즈 워크플로가 태그를 push한다 (#10)** — `changesets/action`은 퍼블리시된 패키지가 있을 때만 태그를 push하는데 `publish`가 `changeset tag`라 퍼블리시가 0건이었다. 그래서 워크플로가 초록불로 끝나면서 태그는 러너와 함께 사라졌다. 태그 push 스텝을 명시적으로 추가하고, 퍼블리시 실행에서 태그가 원격에 없으면 `git ls-remote --exit-code`로 잡을 빨간불로 끝낸다 — 조용한 성공을 없앤다.
  - **KIS throttle이 계좌 단위로 공유된다 (#6)** — `_throttle()`이 인스턴스 단위 `monotonic` 상태만 봐서, 새 프로세스의 첫 호출이 대기를 통째로 건너뛰었다. KIS의 "초당 거래건수" 제한은 계좌 단위인데 5개 프로세스가 각자 카운터를 들고 있었고, 잡 시작 직후 `get_balance` 실패가 여기서 나왔다. 마지막 호출 시각을 계좌+모드 단위 파일에 벽시계로 두고 `fcntl.flock`으로 직렬화한다. 파일을 쓸 수 없으면 조용히 인스턴스 단위로 폴백하고, 벽시계 역행에도 간격 이상 자지 않는다. `telegram_agent`는 명령마다 클라이언트를 새로 만들던 것을 모드별 재사용으로 바꾼다.

## 0.5.1

### Patch Changes

- 8abbb1d: 재배치: 진입 후보가 전부 skip된 날 가동률 미달 경고가 나가지 않던 구멍 수정
  
  `run_cash_deploy`는 잔고 실패·후보 0건·시세 실패 등 모든 조기 반환 갈래에서 가동률
  미달을 경고하지만, `select_entries`가 후보를 전부 거른 갈래만 경고 없이 반환했다.
  2026-08-24 시장 레짐 차단으로 09:30~14:30 11틱 내내 가동률 2.7%(목표 70% 미만)인 채
  알림이 한 건도 나가지 않았다. 이제 이 갈래도 실제 skip 사유를 담아 하루 1회 경고한다.
